"use strict";

//Создание объекта
const obj = {
  name: "John",
  age: 25,
  isMarried: false
};

const human = {
  name: "Anton",
  age: 30,
  sex: "man"
};

console.log(obj.name);

//Создание массива
let arrColor = ['orange', 'blue', 'green'];
console.log(arrColor[1]);

//Интерполяция
const category = "toys";

console.log(`https://someurl.com/${category}/5`);

const user = "АнтоХА";

alert(`Привет, ${user}!`);